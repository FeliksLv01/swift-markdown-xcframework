name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: swift-markdown-release
  cancel-in-progress: false

jobs:
  release:
    if: github.ref == 'refs/heads/main'
    runs-on: macos-26
    env:
      COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - run: brew install xcbeautify cocoapods
      - id: guard
        run: echo "tag=$(./Scripts/release-guard.sh)" >> "$GITHUB_OUTPUT"
      - run: ./Scripts/test-patch.sh
      - run: ./Scripts/test-source.sh
      - run: ./Scripts/build-xcframework.sh
      - run: ./Scripts/test-swiftpm.sh
      - run: ./Scripts/package-release.sh
      - run: ./Scripts/update-release-metadata.rb
      - run: ./Scripts/test-cocoapods.sh
      - name: Commit release metadata
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add Package.swift SwiftMarkdownBinary.podspec
          git diff --cached --quiet || git commit -m "chore(release): prepare ${{ steps.guard.outputs.tag }}"
          git push origin HEAD:main
      - name: Tag and publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git tag "${{ steps.guard.outputs.tag }}"
          git push origin "${{ steps.guard.outputs.tag }}"
          gh release create "${{ steps.guard.outputs.tag }}" Markdown.xcframework.zip NOTICE --generate-notes
      - name: Publish CocoaPods when configured
        if: env.COCOAPODS_TRUNK_TOKEN != ''
        run: pod trunk push SwiftMarkdownBinary.podspec --allow-warnings
